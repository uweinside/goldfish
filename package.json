{
  "name": "goldfish",
  "version": "1.0.0",
  "description": "CuePilot - Glanceable presentation timing assistant (Tauri v2 app)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean:incremental": "cmd /c \"if exist src-tauri\\target\\debug\\incremental (rmdir /s /q src-tauri\\target\\debug\\incremental && echo Removed incremental cache) else echo No incremental cache found\"",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "sortablejs": "^1.15.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "@types/sortablejs": "^1.15.9",
    "@vitest/coverage-v8": "^4.1.2",
    "bootstrap": "^5.3.8",
    "happy-dom": "^20.8.9",
    "typescript": "^5.3.0",
    "vite": "^8.0.1",
    "vitest": "^4.1.2"
  }
}
